# The Flutter app's analyzer configuration.
#
# Adds the Flutter-specific lints to the baseline every workspace package
# shares, so app and package code are held to the same rules.
include:
  - package:flutter_lints/flutter.yaml
  - analysis_options_package.yaml

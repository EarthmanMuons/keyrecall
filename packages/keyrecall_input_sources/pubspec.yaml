name: keyrecall_input_sources
description: >-
  What every KeyRecall input source needs in common, and nothing about any
  particular one.
repository: https://github.com/EarthmanMuons/keyrecall/tree/main/packages/keyrecall_input_sources
issue_tracker: https://github.com/EarthmanMuons/keyrecall/issues

publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.8.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.3
  keyrecall_input: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  import_order_lint: ^0.2.2
  lints: ^6.0.0
